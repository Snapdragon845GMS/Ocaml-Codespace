{
  "name": "Full OCaml Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ocamllabs.ocaml-platform"
      ]
    }
  },
  "postCreateCommand": "opam update && opam install -y dune ocaml-lsp-server ocamlformat utop odoc"
}
